import React from "react";

const LoadingSpinner = () => (
  <div className="loading-spinner">
    <div style={{ color: "rgb(207, 12, 126)" }} className="la-ball-clip-rotate">
      <div />
    </div>
  </div>
);

export default LoadingSpinner;
